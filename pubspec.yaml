name: punching_module
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  go_router: ^12.1.1
  dio: ^5.3.3
  shared_preferences: ^2.2.1
  equatable: ^2.0.5
  camera: ^0.10.5
  path_provider: ^2.1.2
  intl: ^0.20.2
  camera_windows: ^0.2.1
  cryptography: ^2.7.0
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/